//
//  Extension + UIImage.swift
//  SparTest
//
//  Created by Георгий Матченко on 11.08.2023.
//

import UIKit

extension UIImage {
    //stories
    static let stories1: UIImage = {
        guard let image = UIImage(named: "stories1") else { return UIImage() }
        return image
    }()
    
    static let stories2: UIImage = {
        guard let image = UIImage(named: "stories2") else { return UIImage() }
        return image
    }()
    
    static let stories3: UIImage = {
        guard let image = UIImage(named: "stories3") else { return UIImage() }
        return image
    }()
    
    static let stories4: UIImage = {
        guard let image = UIImage(named: "stories4") else { return UIImage() }
        return image
    }()
    
    static let stories5: UIImage = {
        guard let image = UIImage(named: "stories5") else { return UIImage() }
        return image
    }()
    
    //category image
    static let category1: UIImage = {
        guard let image = UIImage(named: "category1") else { return UIImage() }
        return image
    }()
    
    static let category2: UIImage = {
        guard let image = UIImage(named: "category2") else { return UIImage() }
        return image
    }()
    
    static let category3: UIImage = {
        guard let image = UIImage(named: "category3") else { return UIImage() }
        return image
    }()
    
    static let category4: UIImage = {
        guard let image = UIImage(named: "category4") else { return UIImage() }
        return image
    }()
    
    static let category5: UIImage = {
        guard let image = UIImage(named: "category5") else { return UIImage() }
        return image
    }()
    
    // recomendation image
    static let recomend1: UIImage = {
        guard let image = UIImage(named: "recomend1") else { return UIImage() }
        return image
    }()
    
    static let recomend2: UIImage = {
        guard let image = UIImage(named: "recomend2") else { return UIImage() }
        return image
    }()
    
    static let recomend3: UIImage = {
        guard let image = UIImage(named: "recomend3") else { return UIImage() }
        return image
    }()
    
    static let recomend4: UIImage = {
        guard let image = UIImage(named: "recomend4") else { return UIImage() }
        return image
    }()
    
    //sales image
    static let sales1: UIImage = {
        guard let image = UIImage(named: "sales1") else { return UIImage() }
        return image
    }()
    
    static let sales2: UIImage = {
        guard let image = UIImage(named: "sales2") else { return UIImage() }
        return image
    }()
    
    static let sales3: UIImage = {
        guard let image = UIImage(named: "sales3") else { return UIImage() }
        return image
    }()
    
    //sweet image
    static let sweet1: UIImage = {
        guard let image = UIImage(named: "sweet1") else { return UIImage() }
        return image
    }()
    
    static let sweet2: UIImage = {
        guard let image = UIImage(named: "sweet2") else { return UIImage() }
        return image
    }()
    
    static let sweet3: UIImage = {
        guard let image = UIImage(named: "sweet3") else { return UIImage() }
        return image
    }()
    
    static let sweet4: UIImage = {
        guard let image = UIImage(named: "sweet4") else { return UIImage() }
        return image
    }()
    
    //qr code
    static let qrCode: UIImage = {
        guard let image = UIImage(named: "qr") else { return UIImage() }
        return image
    }()
}
